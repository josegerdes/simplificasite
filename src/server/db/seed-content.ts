import { Db } from "mongodb";

/**
 * Placeholder — populado na Fase 3 com o site-config singleton + cursos de
 * exemplo (conteúdo real copiado do simplificadoctor.com). Mantido como
 * função própria (chamada do boot e do CLI de seed) desde já pra não mudar
 * a assinatura depois.
 */
export async function seedSiteContent(_db: Db): Promise<void> {
  // implementado na Fase 3
}
